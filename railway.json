{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "cd medicaregp && python manage.py collectstatic --no-input && gunicorn medicaregp.wsgi --bind 0.0.0.0:8000 --workers 1 --threads 8 --timeout 120 --capture-output --log-level info",
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
